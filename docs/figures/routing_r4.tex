% Figure R4: Point evaluation. Cropped to its own bounding box by the
% standalone class, so the output needs no further trimming.
\documentclass[border=2mm]{standalone}
\input{routing_style.tex}
\input{routing_bodies.tex}
\begin{document}
\figRfour
\end{document}
