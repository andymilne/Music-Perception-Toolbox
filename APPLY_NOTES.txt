APPLY NOTES — bundle: everything uncommitted on top of the SA-line HEAD (75041df).

Includes all previously delivered work (periodic culling, entropy budget, docs)
PLUS the accuracy-tied samples-per-sigma increment. Apply over your tree, then
`git add -A`: identical files produce no diff.

TWO FILES TO DELETE (renamed):
  git rm matlab/tests/test_dispatch_sa_cossim.m
  git rm matlab/tests/test_dispatch_sa_eval.m
